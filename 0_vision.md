# 0_vision.md - 프로젝트 비전 및 목표

## 1. 프로젝트 개요
* **프로젝트명**: API Management Portal (나만의 API 정보 관리 포털)
* **핵심 목적**: 차후 프로그램 개발에 필요한 다양한 외부 API 서비스 URL과 사용법 문서 URL을 한곳에서 체계적으로 관리하고, 필요할 때 즉시 참조 및 활용하기 위함.

## 2. 주요 특징 및 가치
1. **3단 레이아웃 UI**: [상단 헤더] - [좌측 사이드바] - [중앙 본문]으로 이어지는 깔끔하고 직관적인 작업 환경
2. **스마트 카테고리 자동 분류 (LLM)**: URL 입력 시 LLM(AI)이 해당 API의 성격을 분석하여 자동으로 적절한 카테고리 태깅
3. **로컬 데이터 보존**: 별도 DB 서버 없이 브라우저 LocalStorage 기반으로 손쉽고 빠르게 데이터 관리
4. **한눈에 보는 메인 대시보드**: 등록된 API 총개수, 카테고리별 현황, 최근 추가된 API를 직관적으로 파악

## 3. 화면 및 메뉴 구성
- **상단 메뉴**: `메인`, `API`
- **좌측 메뉴**: 상단 `API` 선택 시 `API 정보` 메뉴 활성화
- **중앙 본문**:
  - `메인`: 등록 API 통계 대시보드
  - `API 정보`: API 목록 조회, 신규 API 등록 (URL 기반 자동 카테고리 생성), 상세 정보 확인
